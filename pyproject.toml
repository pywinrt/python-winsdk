[build-system]
requires = ["setuptools", "wheel", "scikit-build>=0.17.0", "cmake", "ninja"]
build-backend = "setuptools.build_meta"

[tool.cibuildwheel]
skip = "pp*"
archs = "all"
build-verbosity = 1
